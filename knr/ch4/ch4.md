## All about functions

- Character arrays are terminated by the null terminator
- Macros are in uppercase
- extern keyword is used to mak global variables available to a file thats not the one the global variable was defined
- static keyword - restricts a global variable to its file
