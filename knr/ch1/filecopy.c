
// A program to copy input to output
#include <stdio.h>

int main()
{
    int c;
    c = getchar(); //a value read from keyboard is stored in c
     while(c != EOF)
     {
        putchar(c);
       c = getchar();
     }

     char ci ;
     

     return 0;
}