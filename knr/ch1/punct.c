//write a program that makes corrects grammatical errors by removing spaces before a full stop,comma or question mark,and eliminates double blanks
#include <stdio.h>
#define NOBLANK 'a'

int main ()
{
    int c, prevc;
    prevc = NOBLANK;
    while ((c = getchar()) != EOF)
    {

    }
}